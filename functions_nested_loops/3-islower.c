#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * int _islower(int c) - Entry point
 * @int c: entry point
 * Return: si characteres en minuscule afficher 1 sinon 0
 */
int _islower(int c)
{
	if (_islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
